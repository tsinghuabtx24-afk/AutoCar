# AutoCar

STM32F103 智能小车工程。当前已包含电机、编码器、循迹和双路红外避障模块。

## 控制架构约束

循迹是车辆的默认行为，但不是不可中断的无限循环。避障、交通标志任务、鸣笛、入库等属于上层行为，可以临时抢占底盘；任务完成后，控制权返回循迹。

主循环只负责按优先级仲裁，不实现传感器或任务细节：

1. 紧急停止或故障保护；
2. 红外避障；
3. 已经开始且必须连续完成的任务，例如转弯、鸣笛、入库；
4. 新识别到的任务或标志；
5. 默认循迹。

各模块统一提供短周期可调用的接口，例如 `Module_Handle()` 或 `Module_Step()`。返回值说明本模块是否占用了底盘控制权。主循环只要发现高优先级模块已占用，就不能再调用低优先级模块写电机。

```c README.md
while (1)
{
  if (Emergency_Handle())
  {
    continue;
  }
  if (IrAvoid_Handle())
  {
    continue;
  }
  if (Task_Handle())
  {
    continue;
  }

  LineTracker_Step();
}
```

当前尚未接入完整任务调度器，因此本次测试在红外无障碍时使用 `Car_ForwardRun()` 直行。后续与循迹结合时，只需把该默认动作替换为 `LineTracker_Step()`。

## 模块职责

### `car`

只负责底盘动作和运动学，不判断何时执行动作。上层传入速度、距离、角度和方向。

### `encoder`

只负责计数、速度、距离和旋转角估算。由 SysTick 周期采样，供运动模块读取。

### `line_tracker`

默认导航行为。`LineTracker_Step()` 只处理一次传感器采样和一次控制决策，适合主循环仲裁。

`LineTracker_Run()` 是独立循迹调试入口，内部为无限循环。集成避障或任务后不能在正式主循环调用它，否则上层再也没有机会检查抢占事件。

### `ir_avoid`

是完整的红外避障行为模块，但不是整车主体模块，负责：

- 左右红外发射与 ADC 采样；
- 阈值、迟滞和障碍方向判断；
- 有障碍时蜂鸣；
- 障碍对应一侧的 RGB 红灯闪烁；
- 左侧障碍时向右原地旋转 30°；
- 右侧障碍时向左原地旋转 30°；
- 双侧障碍时先后退 50 mm，重新检测，再按检测结果旋转；
- `IrAvoid_Handle()` 返回是否抢占了底盘。

避障参数当前位于 `Core/Src/ir_avoid.c`，红外阈值和采样参数位于 `Core/Inc/ir_avoid.h`。这些参数均需按实车、电池和场地标定。

## 任务与事件规划

后续建议新增独立的任务管理模块，而不是把逻辑写入循迹或 `main.c`。识别模块只产生事件，任务模块保存状态并执行动作。

| 事件 | 状态变化或动作 | 完成后行为 |
| --- | --- | --- |
| 打开开关 | 进入运行状态 | 正常循迹 |
| 限速标志 | 更新巡线速度上限 | 按限速持续循迹 |
| 解除限速 | 恢复默认速度上限 | 正常循迹 |
| 左/右转标志 | 创建转弯任务并优先执行 | 返回循迹 |
| 鸣笛标志 | 停车，鸣笛两声 | 返回循迹 |
| 1/2 号库 | 创建对应入库任务，停留 2 秒 | 出库或返回循迹，按赛题定义 |
| 红外障碍 | 立即抢占当前导航或普通任务 | 障碍解除后重新仲裁 |

限速是一个持续配置，不应写成阻塞任务；转弯、鸣笛、入库是有开始和结束的状态任务。任务建议至少包含 `IDLE`、`RUNNING`、`DONE`、`FAILED` 状态，以及开始时间、超时和必要参数。

## 编程规则

- 不在可被抢占的正式模块中使用无法退出的 `while (1)`。
- 调试入口和正式集成入口分开保留，不因接入新功能删除已有调试示例。
- 同一周期只能有一个模块最终写电机；优先级高的模块获得控制权后，低优先级模块必须跳过。
- 传感器识别与动作执行分层：识别产生状态或事件，任务模块决定优先级和生命周期，`car` 执行动作。
- 长动作优先逐步改为非阻塞状态机。当前距离和角度接口仍为阻塞式，动作期间 SysTick 编码器正常工作，但其他事件只能在动作结束后重新仲裁。
- 每个动作都应有超时或失败出口，故障时制动停车。
- 参数使用带单位的宏名，例如 `_MS`、`_MM`、`_DEG10`，避免裸数散落在主循环。

## 当前红外避障测试

正式主循环当前执行：

1. 调用 `IrAvoid_Handle()`；
2. 如果返回 1，红外模块已处理底盘，本周期结束；
3. 如果返回 0，车辆以固定速度直线行驶；
4. 后续将固定直行替换为 `LineTracker_Step()`，即可形成“循迹默认运行、避障优先抢占”的基本架构。
