# AutoCar 功能说明

## 一、按键与蜂鸣器

| 操作 | 效果 |
|------|------|
| 按 key1 或 key3 | 蜂鸣器响，led1 亮、led2 灭 |
| 按 key2 | 蜂鸣器停，led1 灭、led2 亮 |

通过 EXTI 外部中断实现，逻辑在 `HAL_GPIO_EXTI_Callback()` 中。上电默认蜂鸣器关、led2 亮。

## 二、运动控制

七个动作函数，时长参数单位为毫秒，除 `Car_Brake()` 外都会在延时结束后自动停机。

| 函数 | 动作 | 左侧 / 右侧 |
|------|------|------------|
| `Car_Forward(time)` | 前进 | 前 / 前 |
| `Car_Backward(time)` | 后退 | 后 / 后 |
| `Car_TurnLeft(time)` | 左转（以左轮为轴） | 停 / 前 |
| `Car_TurnRight(time)` | 右转（以右轮为轴） | 前 / 停 |
| `Car_RotateLeft(time)` | 原地左旋 | 后 / 前 |
| `Car_RotateRight(time)` | 原地右旋 | 前 / 后 |
| `Car_Brake()` | 立即制动 | 停 / 停 |

### 电机接线

| 电机 | 位置 | A 引脚 | B 引脚 |
|------|------|--------|--------|
| M1 | 左前 | PC6 | PC7 |
| M2 | 左后 | PC8 | PC9 |
| M3 | 右前 | PE9 | PE11 |
| M4 | 右后 | PE13 | PE14 |

单个电机：A 高 B 低为正转，A 低 B 高为反转，同为低则停止。

### 极性补偿

左侧两只电机实测接线与右侧相反，用宏统一补偿，动作函数本身不含极性逻辑：

```c
#define MOTOR_LEFT_POLARITY   (-1)   /* M1 / M2 */
#define MOTOR_RIGHT_POLARITY  (1)    /* M3 / M4 */
```

底层 `Motor_Drive(left, right)` 接收车体方向（+1 前、-1 后、0 停）并在此处施加极性。调方向只需改宏：

- 整车前后颠倒 → 两个宏同时取反
- 左右旋转颠倒 → 两个宏交换取值
- 仅单个轮子反向 → 该轮独立接反，需把极性宏拆成四个

## 三、RGB 状态指示

主循环循环执行六个动作，RGB 同步指示当前状态：

| 颜色 | 动作 |
|------|------|
| 红 | 前进 2s |
| 黄 | 后退 2s |
| 绿 | 左转 1s |
| 青 | 右转 1s |
| 蓝 | 左旋转 1s |
| 品红 | 右旋转 1s |
| 灭 | 制动，停 2s 后重复 |

动作之间间隔 0.5s。

左侧 RGB 的红、绿两路实际接线互换，`RGB_SetColor()` 内部交换这两路输出作补偿，使两侧显示同色。

## 四、自定义用法

自定义动作序列：

```c
while (1)
{
  Car_Forward(3000);
  HAL_Delay(500);
  Car_RotateLeft(500);   /* 调整时长控制转角 */
  HAL_Delay(500);
}
```

按键直接控制小车（替换中断回调内容）：

```c
void HAL_GPIO_EXTI_Callback(uint16_t GPIO_Pin)
{
  if (GPIO_Pin == key1_Pin)      Car_Forward(1000);
  else if (GPIO_Pin == key2_Pin) Car_Brake();
  else if (GPIO_Pin == key3_Pin) Car_Backward(1000);
}
```

## 五、注意事项

- `HAL_Delay()` 为阻塞延时，运动期间主程序不响应其他逻辑；中断仍可触发。
- 中断回调中调用运动函数会在中断上下文里长时间阻塞，仅适合演示，正式项目建议改为置标志位、主循环轮询。
- 电机需独立供电，电流不足会导致转速异常或复位。

## 六、编译信息

RAM 1584 B / 64 KB（2.42%），Flash 6212 B / 512 KB（1.18%），无警告。

固件输出：`build/Debug/AutoCar.hex`

## 七、改动文件

- `Core/Inc/main.h` — 电机引脚定义、函数声明
- `Core/Src/main.c` — 按键中断、RGB 控制、运动控制、演示主循环
