# AutoCar 功能说明

## 实现的功能

### 1. 按键控制蜂鸣器
- **按下 key1 或 key3**：启动蜂鸣器
- **按下 key2**：停止蜂鸣器

### 2. LED指示灯
- **蜂鸣器响时**：led1 亮，led2 灭
- **蜂鸣器不响时**：led1 灭，led2 亮

### 3. RGB LED 彩虹色循环
- **发光时间**：每次 0.2 秒
- **停止时间**：每次 0.2 秒
- **彩虹色顺序**：
  1. 红色 (Red)
  2. 黄色 (Yellow)
  3. 绿色 (Green)
  4. 青色 (Cyan)
  5. 蓝色 (Blue)
  6. 品红色 (Magenta)
  
## 技术实现

### 中断处理
- 使用 EXTI 外部中断处理按键输入
- 在 `HAL_GPIO_EXTI_Callback` 回调函数中处理按键事件

### RGB 控制
- 使用 `RGB_SetColor()` 函数统一控制两个 RGB LED
- 在主循环中按彩虹色顺序循环显示

### 初始化
- 系统启动时 led2 默认点亮（蜂鸣器关闭状态）

## 编译信息
- **RAM 使用**：1584 字节 / 64 KB (2.42%)
- **Flash 使用**：5620 字节 / 512 KB (1.07%)
